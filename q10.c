#include <stdio.h>
int main(int argc, char const *argv[])
{
    int x, y, res;
    printf("Enter a no: ");
    scanf("%d", &x);
  
  x=x/10;
  printf("Result: %d\n",x*10);

    return 0;
}
